{{-- <div class="" id="flashMess">
    <div class="fixed top-0 left-0 flex items-center w-full h-full bg-black z-100 bg-opacity-60 justify-items-center ease-in-out transition duration-300" id="bgFlash">
        <div id="modalBodyFlash" class="m-auto bg-white rounded-md w-11/12 lg:w-6/12 max-h-fit overflow-y-auto ease-in-out transition duration-300">
            <div class="p-4 border">
                @if(session()->has('title'))
                    <div class="text-center p-2 mb-4">
                        <h1 class="font_size10 text-bold uppercase">{{session('title')}}</h1>
                    </div>
                @endif
                <div class="text-center">
                    {{ $message }}
                </div>
                <div class="d-flex justifycontent--end mt-4">
                    <p id="btnCloseModalFlash" onclick="closeModalFlash();" class="">OK</p>
                </div>
            </div>
        </div>
    </div>
</div> --}}
